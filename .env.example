# Sourcegraph MCP Server Configuration
# Copy this file to .env and fill in your values

# Sourcegraph instance URL (default: https://sourcegraph.com)
SRC_ENDPOINT=http://localhost:7080

# Sourcegraph access token (required)
# Get this from your Sourcegraph instance: User Settings > Access Tokens
SRC_ACCESS_TOKEN=redacted-replace-with-your-actual-token

# Optional: Request timeout in milliseconds (default: 30000)
# TIMEOUT_MS=30000

# Optional: Log level (error, warn, info, debug) (default: info)
# LOG_LEVEL=info
